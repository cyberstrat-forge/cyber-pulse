# api.sh 修复实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 api.sh 的 items 命令问题并添加 validate 命令支持

**Architecture:** 移除有问题的 items 命令（调用不存在的端点），添加 sources validate 子命令

**Tech Stack:** Bash, curl, jq

---

## 文件结构

| 文件 | 操作 | 说明 |
|------|------|------|
| `scripts/api.sh` | Modify | 删除 items 命令，添加 validate 命令 |

---

### Task 1: 删除 items 相关函数

**Files:**
- Modify: `scripts/api.sh:1041-1113`

- [ ] **Step 1: 删除 cmd_items 函数（第 1041-1054 行）**

删除以下代码：

```bash
cmd_items() {
    local subcommand="${1:-list}"
    shift || true

    case "$subcommand" in
        list)   cmd_items_list "$@" ;;
        get)    cmd_items_get "$@" ;;
        *)
            print_error "Unknown items subcommand: $subcommand"
            print_items_help
            exit 1
            ;;
    esac
}
```

- [ ] **Step 2: 删除 cmd_items_list 函数（第 1056-1092 行）**

删除以下代码：

```bash
cmd_items_list() {
    local status_filter=""
    local source_id=""
    local limit="50"

    while [[ $# -gt 0 ]]; do
        case "$1" in
            --status)   status_filter="$2"; shift 2 ;;
            --source)   source_id="$2"; shift 2 ;;
            --limit)    limit="$2"; shift 2 ;;
            *)          shift ;;
        esac
    done

    local endpoint="/api/v1/items"
    local params=("limit=$limit")

    [[ -n "$status_filter" ]] && params+=("status=$status_filter")
    [[ -n "$source_id" ]] && params+=("source_id=$source_id")

    endpoint="${endpoint}?$(IFS='&'; echo "${params[*]}")"

    local response
    response=$(api_get "$endpoint")
    check_api_error "$response"

    echo "$response" | jq -r '
        if .data then
            ["ID", "Title", "Status", "Source", "Published"],
            ["--", "-----", "------", "------", "---------"],
            (.data[] | [.item_id[:20], (.title[:40] // "-"), .status, (.source_id[:12] // "-"), (.published_at[:10] // "-")])
            | @tsv
        else
            .[]
        end
    ' | column -t -s $'\t'
}
```

- [ ] **Step 3: 删除 cmd_items_get 函数（第 1094-1106 行）**

删除以下代码：

```bash
cmd_items_get() {
    local item_id="${1:-}"

    if [[ -z "$item_id" ]]; then
        die "Usage: api.sh items get <item_id>"
    fi

    local response
    response=$(api_get "/api/v1/items/$item_id")
    check_api_error "$response"

    echo "$response" | jq .
}
```

- [ ] **Step 4: 删除 print_items_help 函数（第 1108-1113 行）**

删除以下代码：

```bash
print_items_help() {
    echo ""
    echo "Items commands:"
    echo "  list [--status STATUS] [--source SOURCE_ID] [--limit N]"
    echo "  get <item_id>"
}
```

---

### Task 2: 删除 main 函数中的 items case

**Files:**
- Modify: `scripts/api.sh:1205-1208`（行号可能因删除而偏移）

- [ ] **Step 1: 删除 items case 分支**

找到 main 函数中的以下代码并删除：

```bash
        items)
            load_config
            cmd_items "$@"
            ;;
```

---

### Task 3: 删除 show_help 中的 items 帮助信息

**Files:**
- Modify: `scripts/api.sh` show_help 函数

- [ ] **Step 1: 删除 items 相关帮助信息**

找到 show_help 函数中的以下代码并删除：

```bash
    echo "  items <cmd>            内容管理"
    echo "    list                 列出内容"
    echo "    get <id>             获取内容详情"
```

---

### Task 4: 添加 cmd_sources_validate 函数

**Files:**
- Modify: `scripts/api.sh`（在 cmd_sources_cleanup 之后、print_sources_help 之前）

- [ ] **Step 1: 添加 cmd_sources_validate 函数**

在 `cmd_sources_cleanup()` 函数之后、`print_sources_help()` 函数之前插入：

```bash
cmd_sources_validate() {
    local source_id="${1:-}"

    if [[ -z "$source_id" ]]; then
        die "Usage: api.sh sources validate <source_id>"
    fi

    print_info "Validating source: $source_id"

    local response
    response=$(api_post "/api/v1/admin/sources/${source_id}/validate")
    check_api_error "$response"

    local is_valid
    is_valid=$(echo "$response" | jq -r '.is_valid')

    if [[ "$is_valid" == "true" ]]; then
        print_success "Validation passed"
    else
        print_warning "Validation failed"
    fi

    echo ""
    echo "$response" | jq .
}
```

---

### Task 5: 更新 cmd_sources 函数添加 validate case

**Files:**
- Modify: `scripts/api.sh` cmd_sources 函数

- [ ] **Step 1: 在 cmd_sources case 语句中添加 validate**

在 `cleanup)` case 之后添加：

```bash
        validate)       cmd_sources_validate "$@" ;;
```

修改后的 case 语句应为：

```bash
    case "$subcommand" in
        list)           cmd_sources_list "$@" ;;
        get)            cmd_sources_get "$@" ;;
        create)         cmd_sources_create "$@" ;;
        update)         cmd_sources_update "$@" ;;
        delete)         cmd_sources_delete "$@" ;;
        test)           cmd_sources_test "$@" ;;
        schedule)       cmd_sources_schedule "$@" ;;
        unschedule)     cmd_sources_unschedule "$@" ;;
        import)         cmd_sources_import "$@" ;;
        export)         cmd_sources_export "$@" ;;
        defaults)       cmd_sources_defaults "$@" ;;
        set-defaults)   cmd_sources_set_defaults "$@" ;;
        cleanup)        cmd_sources_cleanup "$@" ;;
        validate)       cmd_sources_validate "$@" ;;
        *)
            print_error "Unknown sources subcommand: $subcommand"
            print_sources_help
            exit 1
            ;;
    esac
```

---

### Task 6: 更新 print_sources_help 添加 validate 说明

**Files:**
- Modify: `scripts/api.sh` print_sources_help 函数

- [ ] **Step 1: 在帮助信息中添加 validate 命令**

在 `test` 命令说明之后添加：

```bash
    echo "  validate <source_id>                    验证源质量"
```

修改后的 print_sources_help 函数应为：

```bash
print_sources_help() {
    echo ""
    echo "Sources commands:"
    echo "  list [--status STATUS] [--tier TIER] [--scheduled BOOL]"
    echo "  get <source_id>"
    echo "  create --name NAME --type TYPE --url URL [--tier TIER]"
    echo "  update <source_id> [--name NAME] [--url URL] [--tier TIER] [--status STATUS]"
    echo "  delete <source_id>"
    echo ""
    echo "  test <source_id>                          测试源连接"
    echo "  validate <source_id>                      验证源质量"
    echo "  schedule <source_id> --interval SECONDS   设置采集调度"
    echo "  unschedule <source_id>                    取消采集调度"
    echo ""
    echo "  import --file FILE.opml [--skip-invalid]  批量导入"
    echo "  export [OUTPUT_FILE]                      导出源配置"
    echo ""
    echo "  defaults                                  查看默认配置"
    echo "  set-defaults --interval SECONDS           设置默认采集间隔"
    echo ""
    echo "  cleanup                                   清理已删除的源（物理删除）"
}
```

---

### Task 7: 验证修改

**Files:**
- Test: `scripts/api.sh`

- [ ] **Step 1: 验证脚本语法正确**

```bash
bash -n scripts/api.sh
```

预期：无输出（语法正确）

- [ ] **Step 2: 验证 items 命令已移除**

```bash
./scripts/api.sh items list
```

预期：输出 "Unknown command: items"

- [ ] **Step 3: 验证 validate 命令在帮助中显示**

```bash
./scripts/api.sh --help | grep validate
```

预期：输出包含 "validate <source_id>"

- [ ] **Step 4: 验证 sources 子命令帮助**

```bash
./scripts/api.sh sources invalid_command
```

预期：输出包含 "validate <source_id>"

---

### Task 8: 提交更改

**Files:**
- Commit: `scripts/api.sh`

- [ ] **Step 1: 提交修改**

```bash
git add scripts/api.sh
git commit -m "fix(scripts): remove broken items command and add sources validate

- Remove items command (calls non-existent endpoint GET /api/v1/items/{item_id})
- Remove items list with mismatched parameters
- Add sources validate subcommand for quality validation

Fixes: #87"
```

---

## 自审

- ✅ **Spec coverage:** 所有设计要求都已实现
- ✅ **Placeholder scan:** 无 TBD/TODO
- ✅ **Type consistency:** 不适用（Bash 脚本）