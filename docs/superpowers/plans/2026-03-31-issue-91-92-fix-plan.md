# Issue #91 & #92 修复实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Issue #91（Client 物理删除）和 Issue #92（Scheduler 创建 Job 记录），并清理遗留代码。

**Architecture:** 
1. 新增 `JobTrigger` 枚举区分 job 触发来源（manual/scheduler/create）
2. Client DELETE 端点从软删除改为物理删除
3. 所有采集触发点统一创建 job 记录

**Tech Stack:** Python 3.11+, SQLAlchemy 2.0, FastAPI, APScheduler, Dramatiq

---

## 文件结构

| 文件 | 责任 | 改动类型 |
|------|------|----------|
| `models/job.py` | JobTrigger 枚举 + trigger 字段 | 修改 |
| `models/__init__.py` | 导出 JobTrigger | 修改 |
| `api/schemas/job.py` | JobResponse 添加 trigger | 修改 |
| `api/schemas/item.py` | 移除内部字段 | 修改 |
| `api/routers/admin/jobs.py` | build_job_response + create_job | 修改 |
| `api/routers/admin/sources.py` | create_source 创建 job | 修改 |
| `api/routers/admin/clients.py` | 物理删除实现 | 修改 |
| `api/routers/items.py` | 移除字段赋值 | 修改 |
| `api/auth.py` | 删除 revoke_client 方法 | 修改 |
| `scheduler/jobs.py` | collect_source/run_scheduled_collection | 修改 |
| `api/routers/clients.py` | 未使用的遗留文件 | 删除 |
| `api/routers/__init__.py` | 移除 clients 导入 | 修改 |
| `scripts/build-deploy-package.sh` | 移除 sources.yaml | 修改 |
| `alembic/versions/xxx` | trigger 字段迁移 | 新增 |

---

## Task 1: 添加 JobTrigger 枚举和 trigger 字段

**Files:**
- Modify: `src/cyberpulse/models/job.py`
- Modify: `src/cyberpulse/models/__init__.py`

- [ ] **Step 1: 添加 JobTrigger 枚举到 models/job.py**

在 `JobStatus` 枚举之后添加：

```python
class JobTrigger(StrEnum):
    """Job trigger source enumeration."""
    MANUAL = "manual"      # 手动触发: POST /jobs
    SCHEDULER = "scheduler"  # 定时触发: APScheduler
    CREATE = "create"      # 创建源自动触发
```

在 `Job` 类中添加 trigger 字段（在 `retry_count` 字段之后）：

```python
    # Tracking
    retry_count: Mapped[int] = mapped_column(default=0)
    # Trigger source
    trigger: Mapped[str | None] = mapped_column(String(20), nullable=True)
    started_at: Mapped[datetime | None] = mapped_column()
```

- [ ] **Step 2: 导出 JobTrigger 到 models/__init__.py**

修改导入语句：

```python
from .job import Job, JobStatus, JobType, JobTrigger
```

修改 `__all__` 列表：

```python
__all__ = [
    "ApiClient",
    "ApiClientStatus",
    "Base",
    "TimestampMixin",
    "Item",
    "ItemStatus",
    "Source",
    "SourceStatus",
    "SourceTier",
    "Job",
    "JobType",
    "JobStatus",
    "JobTrigger",  # 新增
    "Settings",
]
```

- [ ] **Step 3: 验证导入正确**

```bash
uv run python -c "from cyberpulse.models import JobTrigger; print(JobTrigger.MANUAL)"
```

Expected: `JobTrigger.MANUAL`

- [ ] **Step 4: Commit**

```bash
git add src/cyberpulse/models/job.py src/cyberpulse/models/__init__.py
git commit -m "feat(models): add JobTrigger enum and trigger field"
```

---

## Task 2: 更新 JobResponse Schema

**Files:**
- Modify: `src/cyberpulse/api/schemas/job.py`

- [ ] **Step 1: 添加 trigger 字段到 JobResponse**

在 `source_name` 字段之后添加：

```python
class JobResponse(BaseModel):
    """Job response."""
    job_id: str
    type: str  # "INGEST" or "IMPORT"
    status: str  # "PENDING", "RUNNING", "COMPLETED", "FAILED"
    source_id: str | None = None
    source_name: str | None = None
    trigger: str | None = None  # 新增：触发来源
    file_name: str | None = None
    result: dict[str, Any] | None = None
    error: dict[str, str] | None = None
    retry_count: int = 0
    created_at: datetime | None = None
    started_at: datetime | None = None
    completed_at: datetime | None = None
    duration_seconds: int | None = None
```

- [ ] **Step 2: Commit**

```bash
git add src/cyberpulse/api/schemas/job.py
git commit -m "feat(schemas): add trigger field to JobResponse"
```

---

## Task 3: 更新 admin/jobs.py

**Files:**
- Modify: `src/cyberpulse/api/routers/admin/jobs.py`

- [ ] **Step 1: 添加 JobTrigger 导入**

修改导入语句（第12行附近）：

```python
from ....models import Job, JobStatus, JobType, JobTrigger, Source
```

- [ ] **Step 2: 更新 build_job_response 函数**

在 `build_job_response` 函数中添加 trigger 字段（第57行附近）：

```python
def build_job_response(job: Job, source_name: str | None = None) -> JobResponse:
    """Build JobResponse from Job model."""
    duration_seconds = None
    if job.started_at and job.completed_at:
        delta = job.completed_at - job.started_at
        duration_seconds = int(delta.total_seconds())

    error = None
    if job.status == JobStatus.FAILED:
        error = {
            "type": job.error_type or "unknown",
            "message": job.error_message or "Unknown error",
        }

    return JobResponse(
        job_id=job.job_id,
        type=job.type.value,
        status=job.status.value,
        source_id=job.source_id,
        source_name=source_name,
        trigger=job.trigger,  # 新增
        file_name=job.file_name,
        result=job.result,
        error=error,
        retry_count=job.retry_count,
        created_at=job.created_at,
        started_at=job.started_at,
        completed_at=job.completed_at,
        duration_seconds=duration_seconds,
    )
```

- [ ] **Step 3: 更新 create_job 端点**

在 `create_job` 函数中设置 trigger（第147行附近）：

```python
    # Create job
    job = Job(
        job_id=f"job_{secrets.token_hex(8)}",
        type=JobType.INGEST,
        status=JobStatus.PENDING,
        source_id=request.source_id,
        trigger=JobTrigger.MANUAL,  # 新增
    )
```

- [ ] **Step 4: Commit**

```bash
git add src/cyberpulse/api/routers/admin/jobs.py
git commit -m "feat(api): add trigger support to jobs endpoint"
```

---

## Task 4: 更新 admin/sources.py - 创建源时创建 job

**Files:**
- Modify: `src/cyberpulse/api/routers/admin/sources.py`

- [ ] **Step 1: 添加 JobTrigger 导入**

修改导入语句（第26行附近）：

```python
from ....models import (
    Job,
    JobStatus,
    JobType,
    JobTrigger,  # 新增
    Settings,
    Source,
    SourceStatus,
    SourceTier,
)
```

- [ ] **Step 2: 修改 create_source 函数的采集触发逻辑**

替换第267-276行的代码：

```python
    logger.info(f"Created source: {new_source.source_id}")

    # Trigger initial ingestion with job tracking
    warnings: list[str] = []
    try:
        # Create job record for tracking
        job = Job(
            job_id=f"job_{secrets.token_hex(8)}",
            type=JobType.INGEST,
            status=JobStatus.PENDING,
            source_id=new_source.source_id,
            trigger=JobTrigger.CREATE,
        )
        db.add(job)
        db.commit()
        
        # Trigger Dramatiq task with job_id
        ingest_source.send(new_source.source_id, job_id=job.job_id)
        logger.info(
            f"Triggered initial ingestion for source: {new_source.source_id}, job: {job.job_id}"
        )
    except Exception as e:
        db.rollback()
        logger.error(f"Failed to trigger initial ingestion: {e}", exc_info=True)
        warnings.append("源已创建，但初始采集任务触发失败，请手动检查")

    return build_source_response(new_source, warnings if warnings else None)
```

- [ ] **Step 3: Commit**

```bash
git add src/cyberpulse/api/routers/admin/sources.py
git commit -m "feat(api): create job record when creating source"
```

---

## Task 5: Client 物理删除

**Files:**
- Modify: `src/cyberpulse/api/routers/admin/clients.py`

- [ ] **Step 1: 添加 delete 和 ApiClient 导入**

修改导入语句（第7-10行）：

```python
from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy import delete  # 新增
from sqlalchemy.orm import Session

from ....models import ApiClient, ApiClientStatus  # 添加 ApiClient
```

- [ ] **Step 2: 替换 revoke_client 端点为 delete_client**

替换第177-194行：

```python
@router.delete("/clients/{client_id}", status_code=200)
async def delete_client(
    client_id: str,
    db: Session = Depends(get_db),
    _admin: ApiClient = Depends(require_permissions(["admin"])),
) -> dict:
    """Delete an API client (permanent deletion).
    
    This is a hard delete. The client will be permanently removed
    from the database and cannot be recovered.
    """
    validate_client_id(client_id)

    logger.info(f"Deleting API client: {client_id}")

    result = db.execute(
        delete(ApiClient).where(ApiClient.client_id == client_id)
    )

    if result.rowcount == 0:
        raise HTTPException(status_code=404, detail=f"Client not found: {client_id}")

    db.commit()

    return {"message": f"Client {client_id} deleted"}
```

- [ ] **Step 3: Commit**

```bash
git add src/cyberpulse/api/routers/admin/clients.py
git commit -m "feat(api): change client deletion to hard delete"
```

---

## Task 6: 删除 revoke_client 方法

**Files:**
- Modify: `src/cyberpulse/api/auth.py`

- [ ] **Step 1: 删除 revoke_client 方法**

删除第232-257行（`revoke_client` 方法）：

```python
    def revoke_client(self, client_id: str) -> bool:
        """
        Revoke an API client's access.

        Args:
            client_id: The client ID to revoke

        Returns:
            True if revoked, False if client not found
        """
        client = self.db.query(ApiClient).filter(
            ApiClient.client_id == client_id
        ).first()

        if not client:
            return False

        client.status = ApiClientStatus.REVOKED  # type: ignore[assignment]
        try:
            self.db.commit()
        except SQLAlchemyError as e:
            logger.error(f"Failed to revoke client {client_id}: {e}")
            self.db.rollback()
            raise
        logger.info(f"Revoked API client: {client_id}")
        return True
```

- [ ] **Step 2: Commit**

```bash
git add src/cyberpulse/api/auth.py
git commit -m "refactor(auth): remove unused revoke_client method"
```

---

## Task 7: 更新 scheduler/jobs.py

**Files:**
- Modify: `src/cyberpulse/scheduler/jobs.py`

- [ ] **Step 1: 更新模块文档和导入**

替换文件开头的导入部分：

```python
"""Job functions for the scheduler.

This module contains the job functions that are scheduled by APScheduler.
These jobs trigger Dramatiq tasks for actual processing.

Import order note:
- models imports are safe (no broker dependency)
- ingestion_tasks import triggers worker.py which configures broker
"""

import logging
import secrets
from typing import Any

from ..database import SessionLocal
from ..models import Item, ItemStatus, Job, JobStatus, JobType, Source, SourceStatus
from ..models.job import JobTrigger
from ..services.source_score_service import SourceScoreService
from ..tasks.ingestion_tasks import ingest_source

logger = logging.getLogger(__name__)
```

- [ ] **Step 2: 重写 collect_source 函数**

替换第18-36行：

```python
def collect_source(source_id: str) -> dict[str, Any]:
    """Collect items from a source via Dramatiq task.
    
    Creates a job record for tracking before queuing the task.

    Args:
        source_id: The ID of the source to collect from.

    Returns:
        Dictionary with job result status.
    """
    db = SessionLocal()
    try:
        # Create job record for tracking
        job = Job(
            job_id=f"job_{secrets.token_hex(8)}",
            type=JobType.INGEST,
            status=JobStatus.PENDING,
            source_id=source_id,
            trigger=JobTrigger.SCHEDULER,
        )
        db.add(job)
        db.commit()

        # Send to Dramatiq task queue with job_id
        ingest_source.send(source_id, job_id=job.job_id)

        logger.info(f"Created scheduler job {job.job_id} for source {source_id}")

        return {
            "source_id": source_id,
            "job_id": job.job_id,
            "status": "queued",
            "message": "Collection job queued successfully",
        }
    except Exception as e:
        db.rollback()
        logger.error(f"Failed to create job for source {source_id}: {e}")
        raise
    finally:
        db.close()
```

- [ ] **Step 3: 重写 run_scheduled_collection 函数**

替换第39-79行：

```python
def run_scheduled_collection() -> dict[str, Any]:
    """Run scheduled collection for all active sources.

    Queries database for active sources and queues collection
    jobs for each, creating job records for tracking.

    Returns:
        Dictionary with job result status.
    """
    logger.info("Running scheduled collection for all active sources")

    db = SessionLocal()
    try:
        # Query all active sources
        sources = db.query(Source).filter(
            Source.status == SourceStatus.ACTIVE
        ).all()

        queued_count = 0
        failed_count = 0
        job_ids = []
        
        for source in sources:
            try:
                # Create job record
                job = Job(
                    job_id=f"job_{secrets.token_hex(8)}",
                    type=JobType.INGEST,
                    status=JobStatus.PENDING,
                    source_id=source.source_id,
                    trigger=JobTrigger.SCHEDULER,
                )
                db.add(job)
                db.flush()  # Get job_id without committing

                # Queue task with job_id
                ingest_source.send(source.source_id, job_id=job.job_id)
                job_ids.append(job.job_id)
                queued_count += 1
            except (OSError, ConnectionError) as e:
                # Catch broker/connection errors specifically
                logger.error(f"Failed to queue source {source.source_id}: {e}")
                failed_count += 1
                continue

        db.commit()
        
        logger.info(f"Queued {queued_count} sources for collection ({failed_count} failed)")

        return {
            "status": "completed",
            "sources_count": queued_count,
            "failed_count": failed_count,
            "job_ids": job_ids,
            "message": f"Queued {queued_count} sources for collection ({failed_count} failed)",
        }
    finally:
        db.close()
```

- [ ] **Step 4: Commit**

```bash
git add src/cyberpulse/scheduler/jobs.py
git commit -m "feat(scheduler): create job records for scheduled collection"
```

---

## Task 8: 移除 Items API 内部字段

**Files:**
- Modify: `src/cyberpulse/api/schemas/item.py`
- Modify: `src/cyberpulse/api/routers/items.py`

- [ ] **Step 1: 从 ItemResponse 移除内部字段**

修改 `src/cyberpulse/api/schemas/item.py`，删除 `full_fetch_attempted` 和 `full_fetch_succeeded` 字段：

```python
class ItemResponse(BaseModel):
    """Single item response."""
    id: str = Field(..., description="Item unique identifier")
    title: str = Field(..., description="Normalized title (with fallback to raw title)")
    author: str | None = None
    published_at: datetime | None = None
    body: str | None = None
    url: str | None = None
    completeness_score: float | None = Field(None, ge=0, le=1)
    tags: list[str] = Field(default_factory=list)
    word_count: int | None = Field(None, description="Word count of normalized body")
    fetched_at: datetime | None = None
    source: SourceInItem | None = None
    # Removed: full_fetch_attempted and full_fetch_succeeded (internal fields)
```

- [ ] **Step 2: 从 items.py 路由移除字段赋值**

修改 `src/cyberpulse/api/routers/items.py` 第120-134行：

```python
        data.append(ItemResponse(
            id=item.item_id,
            title=item.normalized_title or item.title,
            author=item.raw_metadata.get("author") if item.raw_metadata else None,
            published_at=item.published_at,
            body=item.normalized_body,
            url=item.url,
            completeness_score=calculate_completeness_score(item),
            tags=item.raw_metadata.get("tags", []) if item.raw_metadata else [],
            word_count=item.word_count,
            fetched_at=item.fetched_at,
            source=source_info,
        ))
```

- [ ] **Step 3: Commit**

```bash
git add src/cyberpulse/api/schemas/item.py src/cyberpulse/api/routers/items.py
git commit -m "refactor(api): remove internal full_fetch fields from Items API"
```

---

## Task 9: 删除遗留文件和更新导入

**Files:**
- Delete: `src/cyberpulse/api/routers/clients.py`
- Modify: `src/cyberpulse/api/routers/__init__.py`

- [ ] **Step 1: 删除未使用的 clients.py**

```bash
rm src/cyberpulse/api/routers/clients.py
```

- [ ] **Step 2: 更新 routers/__init__.py**

修改文件内容：

```python
"""API routers."""
from . import health, sources

__all__ = ["health", "sources"]
```

- [ ] **Step 3: Commit**

```bash
git add src/cyberpulse/api/routers/clients.py src/cyberpulse/api/routers/__init__.py
git commit -m "chore: remove unused routers/clients.py"
```

---

## Task 10: 更新部署包构建脚本

**Files:**
- Modify: `scripts/build-deploy-package.sh`

- [ ] **Step 1: 从 DEPLOY_FILES 移除 sources.yaml**

修改第51-57行：

```bash
# 部署包包含的文件（不包含 src/ tests/ docs/）
DEPLOY_FILES=(
    "scripts/cyber-pulse.sh"
    "scripts/api.sh"
    "deploy/"
    # sources.yaml removed - users should configure via API
    "install-ops.sh"
)
```

- [ ] **Step 2: 更新 copy_files 函数中的 sources.yaml 处理**

修改第98-122行，在 `copy_files` 函数中：

```bash
copy_files() {
    print_info "复制部署文件..."

    mkdir -p "$TEMP_DIR/cyber-pulse"

    for file in "${DEPLOY_FILES[@]}"; do
        if [[ -e "$PROJECT_ROOT/$file" ]]; then
            # 创建父目录
            parent_dir=$(dirname "$file")
            if [[ "$parent_dir" != "." ]]; then
                mkdir -p "$TEMP_DIR/cyber-pulse/$parent_dir"
            fi
            cp -r "$PROJECT_ROOT/$file" "$TEMP_DIR/cyber-pulse/$file"
        fi
    done

    # 创建 sources.yaml 示例文件（空配置）
    cat > "$TEMP_DIR/cyber-pulse/sources.yaml" << 'EOF'
# Cyber Pulse 情报源配置
# 
# 配置方式：
#   1. 通过 API 导入: ./scripts/api.sh sources import < opml_file
#   2. 手动配置: 参考 https://github.com/cyberstrat-forge/cyber-pulse/blob/main/docs/source-config-examples.md
#
# 示例格式：
# sources:
#   - name: "示例 RSS 源"
#     connector_type: rss
#     config:
#       feed_url: "https://example.com/feed.xml"

sources: []
EOF

    # 创建 .version 文件
    if [[ -n "$VERSION" ]]; then
        echo "$VERSION" > "$TEMP_DIR/cyber-pulse/.version"
        print_info "创建 .version 文件: $VERSION"
    fi

    print_success "文件复制完成"
}
```

- [ ] **Step 3: Commit**

```bash
git add scripts/build-deploy-package.sh
git commit -m "chore: remove sources.yaml from deploy package"
```

---

## Task 11: 创建数据库迁移

**Files:**
- Create: `alembic/versions/xxx_add_job_trigger_field.py`

- [ ] **Step 1: 生成迁移文件**

```bash
uv run alembic revision -m "add_job_trigger_field"
```

- [ ] **Step 2: 编写迁移内容**

编辑生成的迁移文件：

```python
"""add job trigger field

Revision ID: <auto_generated>
Revises: a1b2c3d4e5f6
Create Date: 2026-03-31
"""

from alembic import op
import sqlalchemy as sa

# revision identifiers, used by Alembic.
revision = '<auto_generated>'
down_revision = 'a1b2c3d4e5f6'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # Add trigger column to jobs table
    op.add_column('jobs', sa.Column('trigger', sa.String(20), nullable=True))
    
    # Set default value for existing records (assume manual trigger)
    op.execute("UPDATE jobs SET trigger = 'manual' WHERE trigger IS NULL")


def downgrade() -> None:
    # Remove trigger column
    op.drop_column('jobs', 'trigger')
```

- [ ] **Step 3: 验证迁移**

```bash
uv run alembic upgrade head
uv run alembic downgrade -1
uv run alembic upgrade head
```

- [ ] **Step 4: Commit**

```bash
git add alembic/versions/
git commit -m "feat(db): add job trigger field migration"
```

---

## Task 12: 更新测试

**Files:**
- Modify: `tests/test_scheduler/test_scheduler.py`

- [ ] **Step 1: 更新 test_collect_source 测试**

修改 `TestJobFunctions` 类中的 `test_collect_source`：

```python
    def test_collect_source(self):
        """Test collect_source job function creates a job record."""
        with patch("cyberpulse.scheduler.jobs.SessionLocal") as mock_session_local, \
             patch("cyberpulse.scheduler.jobs.ingest_source") as mock_ingest, \
             patch("cyberpulse.scheduler.jobs.secrets") as mock_secrets:
            
            # Mock database
            mock_db = MagicMock()
            mock_session_local.return_value = mock_db
            
            # Mock job
            mock_job = MagicMock()
            mock_job.job_id = "job_test1234"
            
            # Mock secrets.token_hex
            mock_secrets.token_hex.return_value = "test1234"
            
            # Mock ingest_source.send
            mock_ingest.send = MagicMock()

            result = collect_source("src_test123")

            assert result["status"] == "queued"
            assert result["job_id"] == "job_test1234"
            assert result["source_id"] == "src_test123"
            
            # Verify job was added to database
            mock_db.add.assert_called_once()
            mock_db.commit.assert_called_once()
            
            # Verify ingest_source.send was called with job_id
            mock_ingest.send.assert_called_once_with("src_test123", job_id="job_test1234")
```

- [ ] **Step 2: 更新 test_run_scheduled_collection 测试**

```python
    def test_run_scheduled_collection(self):
        """Test run_scheduled_collection creates job records."""
        with patch("cyberpulse.scheduler.jobs.SessionLocal") as mock_session_local, \
             patch("cyberpulse.scheduler.jobs.ingest_source") as mock_ingest, \
             patch("cyberpulse.scheduler.jobs.secrets") as mock_secrets:
            
            # Mock database
            mock_db = MagicMock()
            mock_session_local.return_value = mock_db
            
            # Mock sources
            mock_source = MagicMock()
            mock_source.source_id = "src_test123"
            mock_source.status = SourceStatus.ACTIVE
            mock_db.query.return_value.filter.return_value.all.return_value = [mock_source]
            
            # Mock job
            mock_job = MagicMock()
            mock_job.job_id = "job_test1234"
            
            # Mock secrets.token_hex
            mock_secrets.token_hex.return_value = "test1234"
            
            # Mock ingest_source.send
            mock_ingest.send = MagicMock()

            result = run_scheduled_collection()

            assert result["status"] == "completed"
            assert result["sources_count"] == 1
            assert len(result["job_ids"]) == 1
            
            # Verify commit was called
            mock_db.commit.assert_called_once()
```

- [ ] **Step 3: 运行测试验证**

```bash
uv run pytest tests/test_scheduler/test_scheduler.py -v
```

Expected: All tests pass

- [ ] **Step 4: Commit**

```bash
git add tests/test_scheduler/test_scheduler.py
git commit -m "test(scheduler): update tests for job creation"
```

---

## Task 13: 运行完整测试套件

- [ ] **Step 1: 运行所有测试**

```bash
uv run pytest -v
```

Expected: All tests pass

- [ ] **Step 2: 运行代码检查**

```bash
uv run ruff check src/ tests/
uv run mypy src/ --ignore-missing-imports
```

Expected: No errors

- [ ] **Step 3: 验证 API 端点**

启动服务并测试：

```bash
# 启动服务
./scripts/cyber-pulse.sh deploy --env dev --local

# 测试 Client 删除
./scripts/api.sh configure
./scripts/api.sh clients list

# 测试 Job trigger 字段
./scripts/api.sh jobs list
```

---

## Task 14: 最终提交和版本更新

- [ ] **Step 1: 更新 CHANGELOG**

添加变更记录到 `CHANGELOG.md`

- [ ] **Step 2: 最终 commit**

```bash
git add -A
git commit -m "fix: resolve issues #91 and #92 with job tracking and client deletion"
```

- [ ] **Step 3: 创建 PR**

```bash
git push origin <branch-name>
gh pr create --title "fix: resolve issues #91 and #92" --body "$(cat <<'EOF'
## Summary
- Issue #91: Client deletion changed to hard delete (physical deletion)
- Issue #92: Scheduler and source creation now create job records with trigger field
- Cleanup: Removed unused files, internal API fields, and sources.yaml from deploy package

## Changes
- Added `JobTrigger` enum (manual/scheduler/create) to track job source
- Modified `DELETE /clients/{id}` to physically delete records
- Updated scheduler functions to create job records before triggering tasks
- Removed `full_fetch_attempted` and `full_fetch_succeeded` from Items API
- Removed unused `routers/clients.py` file
- Removed `sources.yaml` from deployment package

## Test plan
- [ ] All tests pass (`uv run pytest`)
- [ ] Client deletion removes records from database
- [ ] Job records created with correct trigger values
- [ ] Items API doesn't expose internal fields

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```